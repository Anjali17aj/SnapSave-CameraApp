# SnapSave-CameraApp
A React Native mobile camera app built with Expo that supports photo capture, zoom control, and camera flipping. Captured photos are stored locally using AsyncStorage. Features a clean UI with intuitive controls including a zoom slider and camera toggle.
